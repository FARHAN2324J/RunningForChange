import Hero from "../components/Events/Hero";
import Notified from "../components/Events/Notified";
import MainLayout from "../components/layout/MainLayout";

const Events = () => {
  return (
    <MainLayout>
      <Hero />
      <Notified />
    </MainLayout>
  );
};

export default Events;
